# Local `npm run dev` only. In Docker / Azure deployments the API URL is
# injected at container start via the API_BASE_URL env var (see
# frontend/Dockerfile + docker-entrypoint.sh). The kiosk launcher needs an
# *absolute* URL it can reach from the student's machine.
VITE_API_BASE_URL=http://localhost:8001/api/v1

